<!-- <div class="container">
<div class="row">
    <div class="col-sm-6 col-sm-offset-3 text-center"> -->
      <div class="form_card text-center">
        <h2>Add a post</h2>
        <%= simple_form_for @post, validate: true do |f| %>
          <%= f.input :place, label: false %>
          <label class="add-photo-box">
            Add a photo
            <span style="display:none;"  id="photo-upload">
              <%= f.input :photo, required: true, multiple: true, :input_html => {:rows => 10} , name: 'picture' %>
            </span>
          </label>

            <img src="" class="hidden post-photo-preview">

          <div class="note-box">
            <%= f.input :note, as: :text, label: false, placeholder: "Add a note" %>
          </div>

          <div class="row">
            <p>What sort of place was it?</p>
            <%= render 'shared/tag_section', tags: @venue_tags, tag_collection: {} %>
          </div>
          <div class="row">
            <p>What is it good to have there?</p>
            <%= render 'shared/tag_section', tags: @meal_tags, tag_collection: {} %>
          </div>
          <div class="row">
            <p>What sort of food is it there?</p>
            <%= render 'shared/tag_section', tags: @food_tags, tag_collection: {} %>
          </div>
          <div class="row">
            <p>What was the vibe of the place?</p>
            <%= render 'shared/tag_section', tags: @vibe_tags, tag_collection: {} %>
          </div>
          <div class="row">
            <p>What is it good for?</p>
            <%= render 'shared/tag_section', tags: @good_for_tags, tag_collection: {} %>
          </div>
            <%= f.submit "Add Post", class: "btn-pink submit", id: "post-entry" %>
        <% end %>
      </div>
<!--     </div>
  </div>
</div>
 -->
    <%= render 'shared/map', markers: @post_markers %>

